# Feedback harvest — 2026-09-05

First harvest. Two client repos read; **29 signatures** triaged, each into exactly one
disposition.

| Disposition | Count |
|---|---|
| `apply` | 17 |
| `stale` | 5 |
| `decline` | 7 |
| `track` | 0 |
| `blocked` | 0 |

Applied fixes landed in commit `53a5eaf`, `quick-dev` 0.14.0 → 0.14.1 and `notion-dev`
0.21.0 → 0.21.1, with `scripts/verify-runtime-feedback.sh` (76 assertion call sites) added
to hold them.

## Clients read

| Client | Sections | Version range observed |
|---|---|---|
| `smart-contracts-foundry` | 10 | 0.9.0 – 0.20.0 |
| `BTC-Gateway` | 19 | 0.12.2 – 0.21.0 |

Both paths resolved and both logs were present; none was skipped.

## Redaction

Applied per `notion-dev:issue-log`'s **Forbidden, without exception** list before anything was
written here or committed under `plugins/`. The client logs do not honour that contract on the
write side, so this was not a formality — the source material carried a full 32-hex database id,
an internal data-source reference in URL form, an email address, a workspace named after a
person, and a Windows checkout path. Each was generalized to its kind or truncated to the `db=…327627` form. One
ticket-title fragment reached a draft plugin edit and was removed before the commit.

Kept, per the same rule: signatures, `Kind`, occurrence counts, timestamps, plugin versions,
ticket keys, truncated database ids, client repo names, PR numbers, and commit shas.

## A note on what this archive is not

`issue-log` records what an agent was still running to record. Three of these entries describe
runs the harness killed; the runs that died before writing anything left nothing here at all.
**Read this as a floor on what went wrong, never as a complete account.**

---

# `apply` — 17

## `unexpected:subagent-report-never-delivered`

- **BTC-Gateway** — 5 occurrences, 2026-08-31 → 2026-09-04, notion-dev 0.17.0 – 0.21.0.

Dispatched subagents signalled `idle_notification` / `idleReason: available` and delivered no
result at all — no report, no partial output, no error. Across five occurrences it took out
three distinct seats: the flow-triage scout, plan review, and the completeness verifier. An
explicit follow-up message restating the required format produced a second contentless idle.

The fourth recurrence was the first outside `/notion-dev:ticket` — a bare skill invocation with
no worktree, no ticket and no plan reproduced it, which is what makes it a host defect rather
than a plugin one. The fifth was the first to take out the **last remaining** review seat.

**Disposition: `apply`.** The plugin cannot make a harness deliver an agent's result, but it
owns how it *classifies* the absence — and every dispatch site described its failure as output
that "lacks the required sections", which does not cover an empty result at all. A caller
reading "the agent went idle" as "the agent finished" records a review that never happened.

**Change:** `plugins/notion-dev/skills/plan-review/SKILL.md:80`,
`plugins/quick-dev/skills/plan-review/SKILL.md:75`,
`plugins/notion-dev/skills/flow-triage/SKILL.md:70` and the `quick-dev` fork,
`plugins/notion-dev/skills/review-and-merge/SKILL.md:863` and the `quick-dev` fork. The retry
triggers now name "returns nothing at all" explicitly.

## `unexpected:subagent-report-withheld-until-requested`

- **BTC-Gateway** — 1 occurrence, 2026-09-04T10:50Z, notion-dev 0.21.0.

Six review-seat subagents each signalled idle with no payload; a single follow-up message
restating the output contract recovered **all six on the first attempt**. Two implementer
subagents in the same run delivered normally with no nudge.

**Disposition: `apply`,** as one change with the entry above — and the pairing is the whole
point. Recorded eight hours before the fifth occurrence of `subagent-report-never-delivered`,
on the same host and the same plugin version, where the identical remedy recovered neither of
two agents. So the two shapes are not one bug described twice, they are not version-separated,
and they are **indistinguishable at the moment of failure**: both present as a contentless
idle, and only the response to the nudge tells them apart.

A rule that stated only this entry would license "nudge once, then trust the result", which
converts the other condition into a false clean. Both halves ship together: nudge once, then
treat a still-empty result as the failure it is.

**Change:** the same sites as above — the nudge and the warning that it is not a remedy are one
paragraph pair, and `verify-runtime-feedback.sh` pins their order.

## `retry-exhausted:plan-review`

- **BTC-Gateway** — 1 occurrence, 2026-09-03T07:44Z, notion-dev 0.20.2.

Zero bytes from both the initial dispatch and the one permitted retry. The plan went unreviewed;
the orchestrator correctly did **not** substitute a self-review and did not report one.

**Disposition: `apply`.** Same root cause and same fix as the two entries above; recorded
separately here because it is the seat-specific evidence that the generic rule was missing.

**Change:** `plugins/notion-dev/skills/plan-review/SKILL.md:80` and the `quick-dev` fork.

## `retry-exhausted:flow-triage-scout`

- **BTC-Gateway** — 1 occurrence, 2026-08-31T05:18Z, notion-dev 0.17.0.

The `Explore` scout signalled idle twice without delivering. The recovery deliberately departed
from the skill: the degradation path scores three dimensions from the description alone and
nulls the other four, but this run had already established the affected files and the relevant
precedent by direct reads before dispatching. Description-only scoring would have understated
the evidence and pushed a 15/24 ticket into the gray zone on a technicality.

**Disposition: `apply`.** Two changes, not one. The zero-byte shape is the fix above; separately,
the degradation path conflates "the scout failed and nothing else is known" with "the scout
failed but the caller already holds equivalent findings", and only the first justifies nulling.

**Change:** `plugins/notion-dev/skills/flow-triage/SKILL.md:74` and the `quick-dev` fork — a
dimension the caller answered directly is scored from that finding and the substitution recorded
on the `DRIFT:` line; a dimension nothing answers is still `null`, so the carve-out is not
licence to invent evidence.

## `retry-exhausted:completeness-verifier`

- **BTC-Gateway** — 3 occurrences, 2026-08-31 → 2026-09-04, notion-dev 0.17.0 – 0.21.0.

Zero bytes from the verifier on both dispatch and retry. The first two occurrences were easy to
read as mostly harmless: charge 1 survived, because the gate resolves citations mechanically,
and on one of them that mechanical pass caught a real defect.

**The third occurrence isolates the loss.** A standalone `review-and-merge` on a hand-opened PR
had no criteria file, so `CRITERIA-TOTAL` was 0 and charge 1 was **vacuous** — nothing to
resolve. The run lost charges 2 and 3 and *only* charges 2 and 3, against a PR body carrying
**eight checkable factual assertions**, every one produced and stated by the author of the
change and none read by an independent party.

**Disposition: `apply`.** The degraded path's every default is stated *per criterion*, and that
indexing silently degrades to a **no-op** at zero criteria: "mark every criterion `blocked` and
file it" yields zero items, so a gate whose charges 2 and 3 never ran reports nothing wrong at
all. Charges 2 and 3 audit the PR's own prose and are independent of whether criteria exist.

The runs where this is worst are the runs where it is most invisible — a standalone invocation
on a hand-opened PR is exactly where the body is most likely to be long, unreviewed prose.

**Change:** `plugins/notion-dev/skills/review-and-merge/SKILL.md:944` and the `quick-dev` fork —
an undispatchable verifier now raises one item for the charges themselves, `blocked — charges 2
and 3 did not run`, in addition to whatever the per-criterion default produced.

The entry's earlier suggestion — that the degraded path distinguish "the verifier never ran"
from "the gate could not resolve the citations" — was **already implemented** before this
harvest and is not re-applied here.

## `fallback:local-code-review`

- **smart-contracts-foundry** — 1 occurrence, 2026-08-17T14:35Z, notion-dev 0.12.0.

Reviewer-request calls returned HTTP 2xx on two rounds, but both submitted **reviews whose body
was exclusively an inability-to-review notice** — no findings, no reviewed-commit marker. Round 1
on an earlier head had succeeded normally with findings. Classified `reason=error` after the one
allowed retry, then switched permanently to the local fallback.

**Disposition: `apply`.** The `codex` profile has had exactly this rule — an exclusivity guard
for a message that is *only* a refusal — since before this log began. The `copilot` profile had
a rule only for a rejected *request*, so a 2xx **review** that is a refusal matched nothing. The
asymmetry was the defect.

Corroborated independently: the fifth recurrence of `subagent-report-never-delivered` records
two copilot refusal reviews on one PR while the same bot had reviewed a different PR on the same
repo normally under an hour earlier — which is why `reason=error` and not `not-configured` is the
correct reading, and the distinction decides whether a later round may retry it.

**Change:** `plugins/notion-dev/skills/review-and-merge/SKILL.md:757` and the `quick-dev` fork.

Note the entry's second observation — that cross-model review covered the pre-fix head only,
not the three fix commits made in response to it — is a property of the round structure, not a
defect: the loop re-triggers on the new head, and this run's local fallback did converge over
two further rounds.

## `unexpected:copilot-pending-check`

- **BTC-Gateway** — 3 occurrences, 2026-08-14 → 2026-08-16, notion-dev 0.12.2.

The REST `requested_reviewers` endpoint returned `{"users":[],"teams":[]}` — a definite,
non-failing read reporting NOT_PENDING — while the request was genuinely live and the review
arrived ~150 s later. The bot is not surfaced under `.users[]` at all on that repo. The read
succeeded, so the documented READ_FAILED branch does not cover it. The issues timeline was the
only surface that confirmed the request had landed, showing `review_requested` with
`requested_reviewer` = `Copilot` — **that** login, not the `[bot]` form.

The recurrence adds a third blind surface: the reviewer-request **POST's own 200 response**
carried `"requested_reviewers": []` on all three attempts. Every signal available at trigger
time reported "nothing landed" simultaneously, with **no error message anywhere**. The run came
within one judgment call of recording `not-configured` and switching permanently to the local
fallback, which would have discarded a working cross-model review that was merely slow.

**Disposition: `apply`.**

**Change:** `plugins/notion-dev/skills/review-and-merge/SKILL.md:589` and
`references/github-api.md:98`, plus both `quick-dev` forks — an empty `requested_reviewers` is
evidence of nothing in either surface; `not-configured` requires an explicit message; and the
timeline `review_requested` event is documented as the confirming read, with its correct login.

The entry also notes this weakens the silence rule downstream — that rule treats "bot still
listed" as proof the request is merely slow, and on such a repo the condition can never be true,
so a slow round is indistinguishable from a dead one. That is stated at the new text too.

## `unexpected:copilot-request-silent-noop`

- **BTC-Gateway** — 1 occurrence, 2026-08-23T00:32Z, notion-dev 0.12.2.

HTTP 200 on two consecutive attempts, each returning the full PR object with an empty
`requested_reviewers`; the bot then appeared in neither the pending list nor as a submitted
review for ~3.5 minutes, after which the review arrived and the round proceeded normally.

**Disposition: `apply`,** as one change with the entry above. Kept as its own signature because
its `Observed` is the cleanest statement of the gap: the three-bucket classification covers a
*failed* request, and a 200 that silently no-ops is not a failed request. An implementation
keying on the empty array would record `not-configured` while the reviewer was merely slow.

**Change:** as above.

## `unexpected:reviewer-poll-silence-misreport`

- **smart-contracts-foundry** — 1 occurrence, 2026-08-17T14:35Z, notion-dev 0.12.0.

A poll that suppressed stderr and treated a failed read as zero responses reported silence after
the full 20-poll window, while the reviewer had submitted a review roughly 5 minutes in. An
unnecessary re-trigger produced a duplicate review on the same commit, and the real round-1
findings surfaced only on a later manual read.

**Disposition: `apply`** — a `stale` candidate that did not survive inspection. The principle
"a failed read is never evidence" is stated three times nearby: at the trigger re-read
(`SKILL.md:619`), at the `$SEEN` baseline validation, and in the `github-api.md` READ_FAILED
branch. It is stated nowhere **at the poll loop itself**, which is precisely where this run went
wrong — and a poll is a loop, so the cheapest loop body is the one that suppresses its own
errors.

**Change:** `plugins/notion-dev/skills/review-and-merge/SKILL.md:716` and the `quick-dev` fork —
a poll read's stderr is never suppressed, a non-zero exit never falls through into the response
count, and `reason=silent` may only be reached from reads that actually succeeded.

## `unexpected:gh-pr-create-body-stdin`

- **BTC-Gateway** — 1 occurrence, 2026-09-03T07:40Z, notion-dev 0.20.2 (PR #82).

`gh pr create` does **not** support `@-` for `--body`. Passed literally it became the entire
body — two characters, exit code 0, no warning — and the prepared description was silently
absent. The PR sat with no description through all three review rounds. Nothing false was
published, but the disclosure the run most wanted a human to see ("acceptance criterion 1 is
unmet") was missing for the entire review window, and it was caught only at the merge gate when
the closeout check read the body back.

**Disposition: `apply`.** Recorded as the orchestrator's `gh` usage rather than the plugin's,
but the command is where the correct invocation belongs — and the read-back assertion the entry
proposes is one line.

**Change:** `plugins/notion-dev/commands/ticket.md:308` and
`plugins/quick-dev/skills/develop/SKILL.md:126`; the `develop` example now creates the PR with
`--body-file` rather than an inline `--body`.

## `unexpected:notion-markdown-escaping`

- **BTC-Gateway** — 1 occurrence, 2026-08-16T20:24Z, notion-dev 0.12.2.

Notion returns the `## Tasks` lines and the `### [<KEY>-<n>] resolved` headings with their
square brackets **backslash-escaped**. An `update_content` whose `old_str` used the unescaped
form failed with `validation_error: No matches found`, and a resolved-entry regex written
against the unescaped form matched **zero** entries on a page that demonstrably has several.

**Disposition: `apply`.** The escape tolerance existed, but only for the **title prefix** — and
the entry's dangerous half is elsewhere: `epic-update` step 1a's idempotency check parses
`## Resolution Log` for an existing entry, so against the unescaped form `already-recorded` can
never fire, a recovery invocation re-runs the filing pass, and a duplicate log entry is
appended. Per-follow-up `PROVENANCE` dedup still prevents duplicate *tickets*, so the damage is
a duplicated entry rather than lost work — but the check is defeated, not degraded.

**Change:** `plugins/notion-dev/skills/ticket-system/SKILL.md:84` carries the generalized
canonical rule (the escaping is a property of Notion's markdown, so it binds every read-back of
a `[<KEY>-<n>]` bracket form, not just the title); the Resolution Log parse and the `## Tasks`
update site each cite it, and `plugins/notion-dev/skills/epic-update/SKILL.md:32` names step 1a
as the reason it matters.

## `unexpected:plan-review`

- **BTC-Gateway** — 1 occurrence, 2026-08-14T22:20Z, notion-dev 0.12.2.

A reviewer emitted a complete, well-formed block whose COVERAGE-MAP carried a `GAP` matched only
by an FYI finding, while its single Required finding covered an unrelated assertion. Its own
prose argued the gap was not actionable in that repo — which is the rubric's own selectivity
rule, so it arguably should not have marked `GAP` at all. The contract check classified the
output unusable and the one mandated retry was spent on it.

**Disposition: `apply`.** The rubric says each `GAP` becomes a finding and separately says to
record a skipped axis and not raise a finding for it; it never says that an item you have
decided not to raise blockingly must therefore not be marked `GAP`. That is the ambiguity that
produced the retry.

**Change:** `plugins/notion-dev/skills/plan-review/references/reviewer-rubric.md:95` and the
`quick-dev` fork — a `GAP` is a commitment to a **blocking** finding; concluding that no cheap
fix exists within the repo's conventions is the same judgment as skipping the axis, not a `GAP`.

The cost was one extra dispatch, not a wrong outcome: the retry's own Required finding — a false
factual claim in a proposed docs edit — was accepted and applied.

## `unexpected:worktree-isolation`

- **BTC-Gateway** — 1 occurrence, 2026-08-16T19:24Z, notion-dev 0.12.2.

A build-flow implementer subagent, dispatched with the worktree as its working directory and an
absolute worktree path in its brief, **also** wrote its edit to the same relative path under the
primary checkout. Both copies received the change; the worktree copy was committed and merged
normally, while the primary-checkout copy sat as an uncommitted modification — invisible to
every gate in the flow, because each of them inspects the worktree and none inspects the
primary. Cleanup's pull then aborted with "Your local changes would be overwritten by merge".

**Disposition: `apply`,** and the ordering fix applied since (see `unexpected:cleanup-order`
below) makes it *more* urgent rather than less: worktree removal now runs **before** the
checkout and pull, so by the time the pull fails, the only record of where a stray edit came
from has already been deleted. A run following "continue with the remaining cleanup steps and
report" literally leaves the edit sitting in the primary checkout indefinitely and
unattributable.

**Change:** `plugins/notion-dev/commands/ticket.md:447` and the `develop` equivalent — a failed
pull is diagnosed against `git status --porcelain` of the primary checkout, naming the offending
paths, before continuing. And `plugins/notion-dev/commands/ticket.md:146` plus the `develop`
equivalent — the primary checkout is asserted unchanged after each build task, which is the only
check positioned to catch the write while the worktree that explains it still exists.

## `unexpected:worktree-remove-submodules`

- **smart-contracts-foundry** — 3 occurrences, 2026-08-16 → 2026-08-17, notion-dev 0.12.0.

`git worktree remove` failed with `fatal: working trees containing submodules cannot be moved or
removed`. The command anticipates one failure mode — untracked leftovers — and prescribes
retrying with `--force`, which resolves this too. But the stated *cause* is too narrow: a repo
that vendors its dependencies as git submodules puts them in **every** worktree it creates, so
the failure is deterministic rather than incidental, and the run confirmed its own prediction
twice more. Three for three.

**Disposition: `apply`.** The remediation was already correct; only the cause was wrong. Cost is
one wasted command per run plus a failure that reads as an anomaly to whoever is watching.

**Change:** `plugins/notion-dev/commands/ticket.md:442`,
`plugins/notion-dev/commands/finalize.md:177`,
`plugins/quick-dev/skills/develop/SKILL.md` and
`plugins/quick-dev/skills/develop/references/environment-setup.md:76`.

## `unexpected:missing-env-local-in-worktree`

- **smart-contracts-foundry** — 2 occurrences, 2026-08-17, notion-dev 0.12.0.

A gitignored local env file exists only in the primary checkout and is absent from every freshly
created worktree, so the repo's local-deploy target failed until it was recreated from the
committed example. A local service the target expects was likewise not running.

**Disposition: `apply`.** The entry says outright that this is arguably out of scope — the
worktree is created correctly and gitignored files are gitignored by design — *and* that a
one-line note would remove the ambiguity cheaply. That combination is exactly the case where a
documentation fix is warranted: the failure **looks** like a deploy regression in the branch
being tested, right before a merge gate, which is the wrong conclusion to draw at the most
expensive possible moment.

Repo-side alternatives (a target that falls back to the example, a committed
`.env.local.example`) are the project's call and are not proposed here.

**Change:** `plugins/notion-dev/commands/ticket.md:138` and
`plugins/quick-dev/skills/develop/SKILL.md:48`.

## `unexpected:clean-tree-precondition`

- **BTC-Gateway** — 1 occurrence, 2026-08-07T09:42Z, notion-dev 0.12.2.

A tracked, harness-managed local settings file was modified — a key added by the coding harness
itself when MCP servers are enabled, not by a user edit. The precondition's exemption list names
only the two init-generated files, so read literally it halts most non-interactive runs on a
modification nobody made. The run proceeded and completed normally through merge; all work
happened in a worktree cut from the base branch, so the modification could not reach the ticket
branch, and it did not.

**Disposition: `apply`.**

**Change:** `plugins/notion-dev/commands/ticket.md:33` — the bullet is restructured so the
exemptions are stated once and declared exhaustive, and `.claude/settings.local.json` joins the
two init-generated files under the same rationale that already justifies them: work happens in a
worktree cut from `origin/<git.baseBranch>`, so dirt in the primary checkout cannot contaminate
the ticket branch.

## `unexpected:notion-db-not-in-authenticated-workspace`

- **smart-contracts-foundry** — 1 occurrence, 2026-09-03, notion-dev 0.20.0.

`notion-fetch` on the configured database id returned 404 `object_not_found`; the data-source
reference returned 404; a query returned 400 `data_source_not_found`. The session was
authenticated to a personal workspace that does not hold the database. Hard stop before any
worktree, branch, status write, or ledger line — the run created nothing.

The entry also records a **collision hazard**: the reachable workspace held a different
project's tickets under the same key prefix, topping out near a lower number, so any relaxation
of the id lookup would silently operate on a foreign project's ticket.

**Disposition: `apply`,** for the diagnosis note only. The plugin's behaviour was already
correct — the hard stop is right, and the dangerous relaxation is already prevented: the logical-key
fallback is DB-scoped and verifies the hit's id before trusting it, and the project scoping
guardrail is the backstop. What was missing is the sentence that turns three confusing 404s into
a one-step diagnosis, and an explicit statement of why the obvious recovery must never be taken.

**Change:** `plugins/notion-dev/skills/ticket-system/SKILL.md:296` — a 404 on the configured
database is almost always a workspace binding rather than a wrong id, diagnosed with
`notion-fetch "self"`; and the lookup is never widened, because ticket-key prefixes are not
globally unique.

---

# `stale` — 5

## `unexpected:cleanup-order`

- **BTC-Gateway** — 3 occurrences recorded in the header, with two further recurrences described
  in the body (the entry's own text calls the last one the "fifth occurrence"), 2026-08-14 →
  2026-08-31, notion-dev 0.12.2.

Phase 9 checked out the base branch in the primary checkout *before* removing the ticket
worktree, which still held that branch after the branch-deleting form of `gh pr merge` ran its
own local cleanup. The
checkout failed, and because `checkout` and `pull` were separate commands the `git pull` still
ran — on whatever branch the primary happened to be sitting on, creating an unintended local
merge commit on an unrelated feature branch. Recurrences 2 and 3 survived only because the
primary happened to already be on the base branch.

The fifth recurrence is the sharpest and, per this skill's rule that a recurrence outranks the
original, the one that decides the entry: it reports a **second consumer**. A `postMergeHooks`
entry that commits and pushes from the primary checkout ran *between* the merge and Phase 9's
worktree removal, so the hook's commit depended on the same accident. The exposure window was
wider than when the defect was first recorded, and a third fix was implied — the hook should run
after worktree removal.

**Disposition: `stale`.** All three fixes are present:

- `plugins/notion-dev/commands/ticket.md:440` — "**The worktree goes first and the primary
  checkout's `checkout` goes last**", with the `fatal: … is already used by worktree` failure
  named as the reason.
- `plugins/notion-dev/commands/ticket.md:428` — the pull is `--ff-only`, called out as required
  rather than stylistic, so a diverged base aborts instead of manufacturing a merge commit.
- `plugins/notion-dev/commands/ticket.md:471` — post-merge hooks run **after** cleanup, with the
  second-consumer failure stated as the reason.

Held by `scripts/verify-post-merge-ordering.sh`.

## `contract-gap:plan-review-agent-authorization`

- **BTC-Gateway** — 1 occurrence, 2026-09-03T00:20Z, notion-dev 0.20.1.

`plan-review`'s "invoking the skill is the request for that agent" clause was unreachable in the
case that mattered: only a run that invokes the skill can read it, and this run decided at the
call site not to invoke it. Phase 4.2(d) then made the skip look sanctioned by naming degradation
as an outcome without saying that a *general default* is not an explicit prohibition.

**Disposition: `stale`.** The entry names its own fix — pure-dev PR #34, notion-dev 0.20.2 — and
it is present: `plugins/notion-dev/commands/ticket.md:221` carries the clause at the call site
where the decision is actually made, and the exemption is narrowed so a general default cannot
be read as an explicit prohibition. Held by `scripts/verify-agent-dispatch.sh`, which pins the
clause in every dispatching skill and at the `ticket.md` call site.

## `partial:record-phase-artifacts-not-written`

- **BTC-Gateway** — 1 occurrence, 2026-09-04T06:22Z, notion-dev 0.20.2 (STO-77, PR #83).

`epic-update` did not run for the ticket at all — five independent checks agree. No persisted
review report, no follow-up packets, no `Follow-up-of:` provenance markers on the three tickets
created, a hand-authored resolution-log entry in place of step 5's format, and `refreshEpicTasks`
never ran. The three follow-ups exist in Notion, so nothing is lost operationally, but every
artifact the flow relies on to *recognise* them later is missing — and a recovery invocation
would find no provenance marker and create duplicates.

**Disposition: `stale`.** This is the one entry a previous, by-hand harvest had already read, and
its fix shipped: `plugins/notion-dev/commands/ticket.md:371` requires the review-report write to
be confirmed rather than assumed, recording `unexpected:review-report-not-persisted` when it was
not, and cites this exact measurement; `plugins/notion-dev/commands/ticket.md:388` asserts the
packet count against the filed count by exact identity, records
`unexpected:followup-packet-missing`, and cites it too.

## `unexpected:dependsOnProperty`

- **smart-contracts-foundry** — 1 occurrence, 2026-08-02T16:38Z, notion-dev 0.9.0.

**Disposition: `stale`,** and the entry says so itself — it carries a correction subsection
(2026-08-04, observed under 0.12.0) recording that the condition was resolved upstream.

Retained here because the entry is the live example of this skill's rule that an entry's stated
cause is evidence, not a finding. Its original mechanism — that Notion self-relations are
inherently symmetric — was **disproved**, and its own correction records that the
drop-and-recreate the claim rested on never took effect (the recreated column came back carrying
the same `propertyUrl`, which is not consistent with a fresh property). Triage that inherited the
entry's conclusion would have "fixed" a mechanism that does not exist.

**Current text:** `plugins/notion-dev/skills/ticket-system/SKILL.md:409` is the canonical
statement — there is no `dependsOnProperty` config key, and the reason is *not* symmetry;
both relation subtypes preserve direction. `skills/issue-log/references/signatures.md:54` records
that the signature was deleted rather than superseded and must not be re-added.

## `unexpected:local-review-agent-terminated`

- **smart-contracts-foundry** — 1 occurrence, 2026-08-17T14:35Z, notion-dev 0.12.0.

A spawned local reviewer terminated early on an upstream overloaded error, producing no findings
and no verdict. The run discarded the unusable result and spawned one replacement without
incrementing the round counter; the replacement returned CLEAN and the loop converged.

**Disposition: `stale`.** This is the local loop's contract-violation rule working exactly as
written, not a defect: `plugins/notion-dev/skills/review-and-merge/SKILL.md:863` — discard an
unusable result, spawn one replacement without incrementing the counter, stop and report if the
replacement also fails.

That same line was **widened** by this harvest to cover an *empty* result and to require the
nudge first (see `unexpected:subagent-report-never-delivered`), but that is a different
condition: this entry's agent errored visibly, which the rule already handled.

---

# `decline` — 7

## `mcp-unavailable:notion [registered, timed out]`

- **smart-contracts-foundry** — 1 occurrence, 2026-08-02T15:33Z, notion-dev 0.9.0.

The server reported connected, but tool listing timed out on every attempt and no Notion tools
were exposed. `/notion-dev:init` stopped before writing config, pending an OAuth cache clear and
reload.

**Split from the same signature in the other client** — the two `Observed` fields describe
different conditions, so they are triaged separately per this skill's rule. Discriminator drawn
from the difference that caused the split.

**Disposition: `decline`.** The cause is the MCP transport, entirely outside the plugin, and the
plugin's behaviour was already correct: it stopped before writing config rather than persisting a
half-resolved schema. No plugin change would have improved this run's outcome, and adding a
retry or a preflight probe here would trade a clean stop for a slower one.

## `mcp-unavailable:notion [never registered]`

- **BTC-Gateway** — 2 occurrences, 2026-08-11 → 2026-08-22, notion-dev 0.12.2.

No `mcp__notion__*` tool was registered in the session at all — not an error response, an absent
surface. The sibling `github` server from the same config registered normally. Four stale proxy
processes from a session four days earlier were still resident under orphaned parents. Hard abort
before Phase 2; no worktree, branch, status change, or ledger line was created.

**Disposition: `decline`.** Same reasoning as its sibling above, and the entry's own framing
supports it: a session-start connection failure is invisible to the command that needs the
server, which learns of it only when a tool it expects is missing — that is a property of the
harness's server lifecycle, not something a skill file can detect earlier. The hard abort with
nothing created is the correct outcome and is what happened.

## `mcp-unavailable:notion-midrun`

- **BTC-Gateway** — 1 occurrence, 2026-08-14T08:37Z, notion-dev 0.12.2.

The server served Phase 1.1 and 2.1, then was deregistered mid-run. Phase 6.5 could not write the
Implementation section, the PR property, or the ready-for-review comment; Phase 8 was likewise
unreachable. The ticket was left In Progress with no PR link while the PR existed and was green.

**Disposition: `decline`.** The entry notes correctly that a session-start guard cannot catch
this, and it names its own remedy — the state is recoverable via `/notion-dev:finalize`. That
path exists and is already the documented exit:
`plugins/notion-dev/commands/ticket.md:494` requires an unrecoverable failure to report the exact
remaining state and the exact commands to resume, `/notion-dev:finalize <pr>` named explicitly.
Nothing in the plugin can keep an MCP server registered, and the recovery it *can* offer is
already there.

## `mcp-error:notion-query-data-sources-usage-limit`

- **BTC-Gateway** — 2 occurrences, 2026-08-21, notion-dev 0.12.2.

`usage_limit: workspace has reached the usage limit for query data source` during
`getEpicContext`'s sibling-status read. Live sibling status was unavailable; the run fell back to
the epic page's own stale snapshot as background context, labeled stale.

**Disposition: `decline`.** The cause is a workspace-level quota with no plugin remedy, the
degradation was graceful, and the fallback was explicitly labeled — which is the honest handling.
Adding a usage-limit-specific branch would encode one provider's quota message into the skill for
no behavioural gain over the existing degradation.

Noted for a future harvest rather than acted on: the run's fallback to the stale `## Tasks`
snapshot sits in tension with `getEpicContext`'s contract, which says to use live children and
**never** that snapshot. The run labeled it stale and used it as background rather than as epic
context, so the contract was not violated — but if this recurs with the snapshot fed in
unlabeled, that is a different entry and a real defect.

## `fallback:defaultAssignee`

- **smart-contracts-foundry** — 1 occurrence, 2026-08-02T16:12Z, notion-dev 0.9.0.

The configured `defaultAssignee` matched no person in the workspace; lookup by both dashed and
undashed forms returned zero results. Automatic assignment was skipped and the run fell through
to the interactive picker.

**Disposition: `decline`.** This is the documented fallback behaving exactly as specified —
`plugins/notion-dev/commands/create-task.md:183` prescribes the warning and the manual pick — and
the cause is a stale value in client config, not a plugin defect. Validating `defaultAssignee` at
init time was considered and rejected: workspace membership changes after init, so an init-time
check cannot make the runtime resolution unnecessary, and it would add a config-write-blocking
network call for a case the runtime already handles well.

## `fallback:github-rest-reads`

- **smart-contracts-foundry** — 1 occurrence, 2026-08-17T14:35Z, notion-dev 0.12.0.

Both REST PR sub-endpoint reads returned not_found for an open, existing PR — reproducibly, with
an explicit owner/repo as well as the placeholder form — while the repo-level endpoint and all
GraphQL queries succeeded throughout the same window. Every review-state read was switched to
GraphQL for the rest of the run; no data was lost.

**Disposition: `decline`.** A GitHub-side anomaly, one occurrence, self-resolving, with no
correctness cost. The skill already documents the GraphQL reviews/reviewThreads path as
load-bearing for thread state, so the substitution the run made was reaching for a mechanism the
skill already specifies; and the standing rule that a failed read is never evidence already
governs what may be concluded from it. Encoding "if REST 404s on an existing PR, use GraphQL"
would pin a transient provider fault into the reference for the next reader to trust.

## `environment:background-task-killed-mid-run`

- **BTC-Gateway** — 3 occurrences, 2026-09-03, notion-dev 0.20.1.

Three long-running background tasks were killed by the harness mid-run with no failure of their
own (`status: killed`, empty output) — two chain builds and a `Monitor` armed on the first
build's completion.

**Disposition: `decline`.** The entry states plainly this is not a plugin fault, and it is not:
the plugin launches no long-running background work of its own, so there is no dispatch site to
harden. Recorded by the client because it changed the *experiment* rather than merely its timing
— a fixture had to be re-scoped twice to fit a foreground timeout, so a harness constraint picked
a parameter before the measurement did. That is a real caveat about that ticket's evidence and it
belongs, as it was placed, in that ticket's own evidence note.

Kept in this archive because it is durable context for reading the other entries from the same
host: on that machine, background work may not survive.

---

## Reset

Per Phase 8, all five dispositions are removed from the client logs after the merge lands — the
`decline` and `stale` items included. Their durable record is this file; leaving them would
guarantee they are re-triaged next harvest, which is the waste this archive exists to end.
