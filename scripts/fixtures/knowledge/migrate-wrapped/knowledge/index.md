---
okf_version: "0.2"
---
# The bundle

Prose that explains the bundle and carries no bullet at all. The shipped index schema
requires a bullet list in every section, so this heading is dropped rather than emitted
as a section that fails the very check the migration has to pass.

# decision/

- [Wrapped frontmatter survives the migration](decision/flow-mapping.md) — the wrapped
  continuation line is part of this bullet, not a separate entry to be truncated away
